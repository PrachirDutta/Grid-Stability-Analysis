Matlab Simulink Modelling of a Synchronous generator connected to renewable source  (PV array) through buses .
It mimics a grid to which a solar farm is connected through a grid inverter.
different parameters of the alternator like rotor speed deviation ,rotor angle deviation while changing the injected power from the Solar farm /PVarray. 
We can set the parameters of the connected load, PV array  to max or to a percentage of the maximum .Run the "Set_parameters_R2018.m" and set the parameters first.
Run the simulation and scope in  different parameters according to need
